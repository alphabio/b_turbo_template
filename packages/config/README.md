# Shared Configurations

Shared ESLint and other configurations for the monorepo.

## ESLint

Use in your package's `.eslintrc.js`:

```javascript
module.exports = {
  extends: ['@b-turbo-template/config/eslint-preset'],
};
```
