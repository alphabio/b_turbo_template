{
  "name": "web",
  "version": "1.0.0",
  "private": true,
  "description": "Example web application",
  "scripts": {
    "dev": "echo 'Add your dev server here (e.g., vite, next, etc.)'",
    "build": "echo 'Add your build command here'",
    "lint": "echo 'No linting configured'",
    "test": "echo 'No tests configured'",
    "clean": "rm -rf dist .next out"
  },
  "dependencies": {
    "@b-turbo-template/ui": "*"
  },
  "devDependencies": {
    "@b-turbo-template/tsconfig": "*",
    "typescript": "^5.3.0"
  }
}
